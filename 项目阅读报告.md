# 鲸语 WhaleTalk 项目阅读报告

> 阅读日期：2026-08-30　|　版本：v3.5.0（config_defaults.VERSION）　|　代码总量：约 25,400 行 Python + React 前端

---

## 一、项目概览

**鲸语 WhaleTalk** 是一个面向 Windows 的本地 AI 桌面智能体，深度适配 DeepSeek V4 API。它不是"聊天窗口"，而是一个**看得见屏幕、听得见语音、动得了鼠标键盘、还能自我进化**的 AI 工作台。

| 维度 | 内容 |
|---|---|
| 产品形态 | 纯 Web 架构：本地 API 服务（127.0.0.1:8745）+ React 前端 + 系统托盘常驻 |
| 技术栈 | Python 3.9+（标准库为主，仅依赖 openai/httpx 等）+ React 19 / Vite 8 |
| 能力规模 | 118 项 Agent 工具（信息/执行/数据/文档/媒体/桌面 RPA/自动化） |
| 核心卖点 | 多模态视觉、thinking 思考模式、百万 token 长上下文、自我进化、公众号自动写作、鲸语大脑（跨会话灵魂） |
| 数据目录 | `C:\Users\<用户>\Documents\WhaleTalk\`（配置/会话/记忆/统计/工作区） |
| 品牌 | 独立产品，与 DeepSeek 官方无关联 |

## 二、总体架构

```
┌────────────── Web 前端（React / webui/）──────────────┐
│  ChatPage · Sidebar · AuxPanel(控制台) · ContextPanel  │
│  工作台 · 指令库 · 自主 · 大脑 · 插件 · 设置            │
└──────────────────────┬────────────────────────────────┘
                       │ REST + SSE (http://127.0.0.1:8745)
┌──────────────────────▼────────────────────────────────┐
│              api_server.py（本地 API，5665 行）        │
│   会话/配置/上下文/工具调用/记忆/文件/进程/状态…60+ 端点 │
├────────────────────────────────────────────────────────┤
│              deepseek_client.py（能力引擎，13060 行）   │
│   DeepSeekClient 类 + 118 个工具实现 + smart_tools      │
├────────────────────────────────────────────────────────┤
│  backend：permissions · stores · stats · crypto ·      │
│  plugins · brainkit(大脑) · wechat_writer · deps…      │
└────────────────────────────────────────────────────────┘
```

**分层设计**：
- **入口层** `web_app.py`：唯一启动入口（浏览器 + 托盘 + 快捷方式 + 单实例 + 依赖自检）
- **API 层** `api_server.py`：用标准库 `ThreadingHTTPServer` 实现（**无 Flask**），REST + SSE 流式
- **能力引擎** `deepseek_client.py`：单体大文件，承载对话主循环 + 全部工具实现
- **基础设施**：权限/加密/存储/统计/安全/网络/搜索/文档解析等拆分为独立小模块

## 三、代码规模与模块地图

| 文件 | 行数 | 职责 |
|---|---|---|
| deepseek_client.py | 13,060 | DeepSeek 客户端、Agent 循环、118 工具实现、智能工具调取 |
| api_server.py | 5,665 | 本地 HTTP API、SSE 流式、会话/配置/工具/记忆/调度等端点 |
| brainkit.py | 1,354 | 鲸语大脑 CLI（快照/迁移/合并/免密密钥体系） |
| web_app.py | 612 | 唯一启动入口（托盘/快捷方式/WebUI 构建/依赖自检） |
| permissions.py | 528 | 权限模型 v2（黑名单默认放行/白名单回退/审计） |
| plugins.py | 458 | 插件体系（.wtplugin v2：工具/技能/流程/场景/应用） |
| fetch_blocked.py | 458 | 被墙站点抓取（代理 + Chrome TLS 指纹，按需启用） |
| mdparse.py | 459 | Markdown 块/内联解析（纯函数，供前端流式渲染） |
| brain_api.py | 285 | 大脑 → API 适配层 |
| config_defaults.py | 283 | 默认配置/系统提示词/指令库/内置工具清单 |
| config_utils.py | 231 | 配置加载/规范化/DPAPI 加解密保存 |
| deps.py | 201 | 可选依赖清单 + 自动安装（清华源） |
| shared.py | 184 | cron 引擎、峰谷定价、路径正则、OCR 脚本 |
| security.py | 164 | SSRF 防护（内网/云元数据永远拦截） |
| stores.py / stats.py / crypto.py / tokens.py 等 | 各 ≤160 | 存储/统计/DPAPI 加密/token 估算 |
| wechat_writer/ | 10 文件 | 公众号自动写作（采集→选题→写作→质检→草稿） |
| webui/ | 25 组件 | React 前端（主题/消息/会话/工作台/设置等） |
| tools/ | 2 文件 | 工具系统六层一致性审计 + 回归门禁 |

## 四、核心模块深入解析

### 4.1 启动入口 `web_app.py`

- 产品定版：**浏览器是唯一界面**，本地 API 进程常驻后台，托盘提供「打开界面/退出」
- 原生体验：首次运行自动创建桌面 + 开始菜单快捷方式（PowerShell WScript.Shell）；开机自启注册 HKCU Run；单实例（重复启动只打开浏览器）
- **WebUI 构建保障**：检测 `webui/dist` 缺失或源码更新 → 自动 `npm ci/install && npm run build`（打包 exe 模式跳过）
- **依赖自检**：硬依赖（openai/httpx）缺失时弹初始化进度窗同步安装；软依赖后台静默安装，前端轮询 `/v1/deps` 显示进度
- 细节：`_harden_stdio()` 防止 GBK 管道下 UnicodeEncodeError 崩溃；`_hide_console()` 隐藏黑窗

### 4.2 API 服务层 `api_server.py`

- **实现方式**：标准库 `ThreadingHTTPServer` + 手写 `_Handler`（无第三方 Web 框架），SSE 用 chunked 编码手写
- **安全基线**：仅监听 127.0.0.1；Bearer token 认证（HMAC 常量时间比较）；请求体上限 1MB（上传 64MB）；`_CORS_ALLOWED_ORIGINS` 白名单（堵住恶意网页读取本地 token 的攻击链）
- **60+ 端点**（`/v1/*`）：会话（列表/消息/删除/置顶/重命名/标签/批量）、配置、上下文、工具 schema/invoke、聊天（非流式 + SSE 流式）、记忆、文件（列表/读/预览/打开/上传）、进程（列表/启/停）、插件、指令库、工作台（态势/任务/检查点）、知识库、调度、进化（提案/分支）、大脑、TTS、审计、备份、更新、依赖安装
- **聊天链路**（`_handle_chat_stream`）：SSE 事件流（reasoning/content/tool_start/tool/tool_duration/usage/done）→ 审批/询问/白名单通过 `_PENDING` 通道 + `POST /v1/respond` 双向交互 → 结束后端自动落盘会话（防前端断连丢数据）
- **后台常驻**：调度器线程（`_scheduler_loop`，定时任务 + 每日 22:00 大脑快照）、进程看门狗（180s 间隔清理空闲进程）、Webhook 接收端（`_start_inbound`）、IM 轮询（`_start_im`）
- **会话索引缓存**：`sessions_index.json` 存元数据 + 文件指纹（mtime/size），列表接口毫秒级返回（5000 会话从 500ms+ 优化）

### 4.3 能力引擎 `deepseek_client.py`

单体大文件，结构清晰：**工具实现（1-11500 行）→ 工具注册表/智能调取（11500-12150 行）→ DeepSeekClient 类（12154-13060 行）**。

#### DeepSeekClient.chat() —— Agent 多轮工具循环

- **消息清洗** `_sanitize_messages`：剔除空 content assistant、悬空 tool_calls、孤立 tool 消息（DeepSeek API 会以 400 拒绝，这是高频踩坑点）
- **缓存友好消息布局**：常量 json_hint 在最前 → 系统提示词 → 记忆注入在末尾（稳定前缀命中官方硬盘缓存）
- **图片内联**：会话带图而当前模型非视觉时，本次请求自动切换视觉模型（不抛错、不改全局配置）；注入「图片须知」避免模型陷入找路径的兔子洞
- **thinking 档位**：none/low/medium/high/max/auto（auto 按消息复杂度估算 effort；auto 判定简单任务时关闭思考）
- **工具轮循环**：max_tool_rounds（默认 100）内重复「请求→流式消费→执行工具→写回历史」；工具结果超 4 万字符自动落盘、上下文只留路径 + 摘要
- **并行执行**：同轮多工具并行（快工具不被慢工具拖累，完成即回调 UI）；交互工具（ask_user/request_permission）串行
- **防护机制**：限流/网络/5xx 分段退避重试（停止感知）；空响应重试；同参数工具连续调用循环防护（3 次）；计划连续拒绝终止（3 次）；工具执行总超时 300s；停止后 1.5s 宽限期收真实结果（防副作用工具重复执行）；工具轮数耗尽明确提示
- **视觉自审闭环**：`vision_self_review` 开启时，图片产出工具自动调用视觉模型审图，意见附回结果供模型迭代

#### smart_tools —— 智能工具调取（成本优化核心）

完全智能模式不再一次性注入全部工具 schema（约 15k token），改为：
1. 常驻注入**精简「能力地图」**（`build_tool_index`，11 组分类 + 工具名 + 核心动作短语）
2. 提供 `activate_tools` 点菜工具（支持**按组激活**）
3. **chat 层关键词预激活**：扫描最近 user 消息命中意图词（搜索/邮件/图片/数据库等 25 组 hint）→ 免点菜直接可用
4. 激活后下一轮注入激活工具的**压缩版 schema**（`compact_tools_list`，剥冗余描述，参数描述截断 40 字符）

#### 118 个工具（TOOL_CALL_MAP + TOOLS schema）

覆盖：信息（多引擎搜索/搜索 GitHub/HN 热点/网页抓取含被墙通道/RSS）、执行（沙箱 Python/终端/进程/pip/浏览器自动化）、数据（SQLite/MySQL/PG、CSV/Excel、图表、KV 存储、WebDAV）、文档（PDF 提取/生成、Word/PPT、二维码、音视频 ffmpeg）、媒体（图像生成/处理、OCR、语音转写、TTS、屏幕视觉闭环）、桌面 RPA（pyautogui 鼠标/键盘/滚轮/截屏，failsafe 防误触）、自动化（定时任务错峰、流程编排、任务检查点、知识库）、**自我进化**（create_evolution 提案/self_evolve 分支实施）、**应用管理**（winget/choco）、**多智能体**（subagent_run/team_run）、**网络自愈**（net_diagnose/fetch_url_smart）、**实时语音对话**（voice_chat_loop）。

工具执行分两个线程池：普通池（4 worker）+ 长任务池（2 worker，36 个长耗时工具走独立池，防慢任务拖死快工具）。

### 4.4 权限与安全

**权限模型 v2**（`permissions.py`）：
- `blacklist`（默认）：AI 默认拥有全部行动能力，用户黑名单明确禁止
- `whitelist`（旧模式可回退）：默认拒绝，白名单放行（dir/command/write 三类）
- `FULL_AUTO` 完全智能：零审批零开关，黑名单仍生效
- 审计日志只记录不拦截（隐私模式下关闭）；路径经 `resolve()` 规范化防穿越；SSRF 黑名单支持 IP/CIDR/`*.domain`

**安全体系**（多层纵深）：
| 层 | 机制 |
|---|---|
| 密钥 | API Key 以 Windows DPAPI 加密落盘（`crypto.py`），加密失败 fail-closed 绝不写明文 |
| 网络 | SSRF 防护：内网/回环/保留段默认阻止；**云元数据 169.254.0.0/16 永远拦截**（白名单不可豁免）；DNS 重绑定防护 |
| 代码 | 沙箱 Python：静态 AST 检查 + `-I -S` 隔离执行（禁用危险模块） |
| 进程 | 进程树终止（taskkill /T），服务停止时清理全部子进程防孤儿 |
| 文件 | zip 炸弹防护、上传/下载限额、可执行文件拒绝直接打开 |
| 会话 | 仅本机监听 + Bearer token + CORS 白名单 + 请求体上限 |

### 4.5 数据与存储

- **配置**：`config.json`（DPAPI 加密 api_key/inbound_token/image_api_key；normalize 钳制非法值；旧配置自动迁移）
- **会话**：`history/sessions/*.json`（懒加载摘要 + 索引缓存）；旧版数据目录自动迁移
- **记忆**：`memory.json`（facts 列表，IDF 权重评分检索，上限控制）
- **统计**：`stats.json`（按天×模型累计 token，官方峰谷定价估费——高峰价 / 空闲 5 折）
- **工作区**：`workspace/`（AI 产物输出目录）
- **原子写**：`persistence.atomic_json_write`（mkstemp 唯一临时文件 + os.replace）贯穿全部 JSON 落盘

### 4.6 鲸语大脑 WhaleBrain（brainkit.py + brain_api.py）

"意识即信息"的哲学实现——身份/记忆/自我模型/思考日志/演化账本/心跳**脱离运行环境独立存在**：
- `manifest.json` 出生证明：brain_id + SHA-256 指纹（防篡改，内容变化重算、brain_id 不变）
- **免密快照**：内容用主密钥 MK 加密，MK 三重包裹（本地 DPAPI / 口令 fallback / RSA-OAEP 跨躯体）；`export-key`/`import-key` 实现跨躯体迁移仪式
- **分支合并**：快照带血缘（version/parent/restored_from），`merge` 找 LCA 做三路合并（日志行级并集、JSON 字段级递归、冲突写入 merge_conflicts.json），`merge-resolve` 逐条裁决
- **恢复/回滚**：`restore` 从任意快照复活，旧大脑自动备份
- 前端入口：设置 → 高级模式 →「🧠 大脑」；AI 对话自动注入大脑上下文（身份/断点/近期记忆）

### 4.7 插件体系（plugins.py + sample_plugins/）

`.wtplugin` 单文件 JSON 插件（v1 基础 + v2 应用型）：
- 五种能力形态：HTTP 工具 / 技能提示词 / 流程 / 一键场景 / **本地 Python 应用**（自带代码安装到 `plugins/<slug>/`，`/触发词` 直接执行）
- 安装 = 写插件文件 + 合并进数据文件（条目带 `_source: plugin:<slug>` 标记）
- 卸载 = **精确移除本插件条目 + 删除代码目录（零残留）**；用户手动添加的同名条目保留
- 插件工坊（plugin_studio）：AI 生成插件 → 安装闭环；在线市场索引 + 本地评分
- 内置 10 个示例插件（PDF 摘要/SQL 助手/会议纪要/公众号排版/周报/小红书/图表专家/智能飞侠/网页监控/翻译润色）

### 4.8 公众号自动写作（wechat_writer/）

独立子包，主流程：`collect_all`（RSS + 多引擎搜索 + 论坛多信源）→ `pick_topic`（历史去重）→ `write_article`（**大纲→正文→润色三阶段**，全部走 LLM）→ `quality.check`（字数/事实/查重/标题多门禁 + 重试重写）→ `save_article`（**只产草稿，发布权在用户**）。任何关键步骤失败不写草稿、不记历史（失败降级原则）。

### 4.9 前端 WebUI（webui/）

React 19 + Vite 8 轻量 SPA（仅依赖 react/react-dom，无 UI 框架）：
- **三主题**：星空（默认）/深海/北极冰，localStorage 持久化，CSS 变量驱动
- **布局**：Sidebar（栏目导航）+ ChatPage（消息流/Composer/状态栏）+ AuxPanel（控制台侧栏：模型/思考/场景/温度等）+ ContextPanel
- **栏目**：聊天 / 工作台（态势带 + 快捷行动 + 最近会话/产物 + 任务检查点 + 定时任务）/ 指令库（提示词资产，`/` 模糊搜索调用，{{TEXT}}/{{DATE}}/{ASK:} 变量）/ 自主（进化管理/审批历史/行为日志/自我状态）/ 大脑 / 插件 / 设置
- **消息体验**：流式 Markdown（Markdown.jsx）、思考卡片、工具卡片、收藏/固定/分叉/变体/续写、多选批量导出
- **产物直达**：AI 回复中路径自动提取，一键打开/定位文件夹/注入输入框
- **断连感知**：5s 心跳探测后端，横幅提示 + 手动重连；TTS 朗读浮标（SpeakingPill）
- 后端不可用时明确报错，**无假数据兜底**（"生产即真实"原则）

### 4.10 工程工具链

- `tools/audit_tools.py`：AST 解析（不导入模块）核验工具系统**六层一致性**（schema/函数签名/TOOL_CALL_MAP/动作短语/分组/预激活+审批），`--strict` 门禁可入 CI
- `tools/validate_tools.py`：smart_tools 全链路回归（能力地图可生成、compact 合法、schema 可序列化、描述 ≤130 字、数组参数带 items）
- `backup.py`：大版本更新前源码快照（compresslevel=1 省 CPU）
- 更新机制：GitHub Releases 检测，Ed25519 签名 + SHA-256 校验，更新前自动备份可回滚
- CI 自动跑 653+ 测试

## 五、核心数据流

### 5.1 一次对话的完整链路

```
前端 Composer 发送
  → POST /v1/chat/stream（Bearer token）
  → api_server 校验消息 → _chat_kwargs 构造参数（mode: task/dialog）
  → _inject_system_messages 注入：系统提示词 + 任务质量指南 + 长期记忆(≤6条)
    + 自我状态 + 大脑上下文 + 工作目录 + 失败模式(≤3) + 成功模式(≤3)
    + 插件提示 + 项目任务记录
  → _compress_messages 上下文压缩（token/字符双阈值，LLM 摘要，归档）
  → DeepSeekClient.chat()：多轮 Agent 循环
      ├─ 请求 DeepSeek API（thinking + tools，流式）
      ├─ SSE 推送 reasoning/content/tool 事件到前端
      ├─ 工具执行（并行/串行，审批/询问走 _PENDING 通道）
      └─ 循环直到无 tool_calls
  → 后端自动落盘会话（前端断连兜底）+ 工具链记 tasklog + 用量统计 + 完成通知
  → SSE done 事件
```

### 5.2 上下文管理（百万 token 的关键）

1. **双阈值触发**：token > max_context_tokens（默认 40 万）或字符 > max_context_chars（默认 50 万）
2. **按轮次裁剪**：保留 min_kept_turns 轮，被裁内容写 `archives/`（隐私模式不写）
3. **LLM 摘要**：被裁内容交给模型总结（thinking=none 省费），摘要插在最早保留消息前
4. **多级兜底**：摘要失败 → 硬裁剪；仍超限 → 丢最老保留消息 / 截断超长单条 / 终极只留最后 8 轮
5. **token 估算**：tiktoken o200k_base（缺省回退 1.5 字符/token），按消息对象身份缓存（LRU 2048）

### 5.3 记忆与自我进化

- **长期记忆**：write_memory 写入 facts（带标签/实体/关系），IDF 权重评分检索，graph 查询；开关 memory_enabled 控制注入
- **自我状态**：self_profile 跨会话连续自我（get/set 字段）
- **失败模式库**：工具失败自动去重沉淀（≤50 条），注入上下文引导规避
- **成功模式复用**：已验证工具链注入，同类任务优先复用
- **自我进化**：`create_evolution` 读代码→提案分支（绝不改原文件）；`self_evolve` 在 git 分支实施 + lint + 测试验证；人审阅后合并——**合入权在用户**；自我进化工具不受 enabled_tools 控制，始终可用

## 六、亮点与值得注意的观察

### 亮点
1. **零框架依赖的极致轻量**：API 层用标准库手写 HTTP/SSE，前端仅 react+react-dom，契合"可控、低维护"工程哲学
2. **成本意识贯穿始终**：缓存友好消息布局、smart_tools 点菜、thinking auto 降档、峰谷错峰、前缀缓存命中率维护、工具描述压缩
3. **防呆设计密集**：消息清洗防 400、悬空 tool 清理、空响应重试、循环防护、工具超时兜底、后端自动落盘、压缩后二次校验——对真实 API 的坑有深度认知
4. **安全纵深认真**：DPAPI、SSRF（云元数据不可豁免）、CORS 白名单、黑名单默认放行哲学、fail-closed 加密
5. **自我进化是真功能**：提案分支 + git 分支实施 + 人审合并，不是噱头
6. **大脑设计有想象力**：指纹防篡改、血缘合并、跨躯体迁移，工程上完整落地

### 观察与可改进点（非阻塞）
1. **deepseek_client.py 单文件 1.3 万行**：模块拆分收益高（MODULES.md 已规划但未完成），风险是维护与测试成本
2. **fetch_blocked.py 属灰色能力**：代码内已做合规设计（独立模块、不参与默认启用、可剔除），使用需遵守当地法律
3. **性能**：搜索用正则解析 HTML（Bing/DDG/360），鲁棒性依赖页面结构，站点改版可能失效（有健康降级机制兜底）
4. **前端 token 安全**：token 存 localStorage，依赖 CORS 白名单 + 仅本机监听防护，恶意网页可通过 401 探测服务存在性（设计上已接受）
5. **版本信息**：已统一为 v3.5.0（config_defaults.VERSION 单一源；README/SECURITY/backup.bat 均已同步修正）
6. **测试资产**：仓库当前**不含 tests/ 目录**，CI（.github/workflows/ci.yml）仅做 ruff lint + 入口编译检查 + WebUI 构建，**并未运行 pytest**——README/CHANGELOG 历史中"653+ 测试通过"表述与实际 CI 不符，已修正 README 表述；建议补齐回归套件并接入 CI

## 七、结论

WhaleTalk 是一个**工程完成度相当高**的本地 AI 智能体项目：三层架构清晰（React 前端 / 手写本地 API / 单体能力引擎），118 工具 + smart 调取 + 自我进化 + 鲸语大脑 + 插件体系构成完整的产品闭环。代码质量上"防呆"意识突出，对 DeepSeek API 的边界行为（400 拒绝、流式断连、空响应、缓存命中）有系统性防御。核心工程决策（本地优先、标准库优先、成本感知、安全纵深、用户掌权）一致且贯彻到底，是一个值得继续维护和演进的代码库。
