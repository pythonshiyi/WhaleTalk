# ── 鲸语 WhaleTalk 核心依赖（31 项，与 deps.py AUTO_INSTALL_DEPS 保持一致）──
# 首次启动自动安装全部核心组件；大型可选能力（浏览器自动化/语音转写/Piper 语音/二维码识别/RAR）
# 不在此清单，进入程序后在「设置 → 🔌 可选能力」按需一键安装。

# 核心 API 网关
openai>=1.75.0
httpx>=0.27.0
# 系统托盘常驻
pystray>=0.19.5
pywin32>=306
# 图片处理（image_process / 截图 / 图标）
Pillow>=10.0
# 精确 token 估算
tiktoken>=0.7.0
# 代码块语法高亮
pygments>=2.15
# 公众号写作 HTML 输出
markdown>=3.5
# 嵌入式 KV 存储（kv_store）
diskcache>=5.6
# 系统资源自检（system_status）
psutil>=5.9.0
# 桌面 RPA（鼠标/键盘/屏幕坐标）
pyautogui>=0.9.54
# PDF 提取
PyMuPDF>=1.24
# PDF 生成
reportlab>=4.0
# Word 读写
python-docx>=1.1.0
# PPT 读取
python-pptx>=0.6.23
# RSS 聚合
feedparser>=6.0
# 二维码生成
qrcode[pil]>=7.4
# 音视频处理（内置 ffmpeg）
imageio-ffmpeg>=0.4.9
# Excel 读写
openpyxl>=3.1.0
# 数据图表
matplotlib>=3.8.0
# 被墙站点抓取（Chrome 指纹，可过 Cloudflare）
curl_cffi>=0.7.0
# 实时语音录音（voice_chat_loop）
sounddevice>=0.4.6
# 在线神经音色（Edge 朗读，缺失自动回退 SAPI）
edge-tts>=6.1.0
# EPUB 电子书阅读
ebooklib>=0.18
# MOBI 电子书阅读
mobi>=0.3.3
# Outlook .msg 邮件阅读
extract-msg>=0.48
# 7z 压缩包
py7zr>=0.20
# 数值计算 / 语音分析
numpy>=1.24.0
# MySQL 只读查询
PyMySQL>=1.1.0
# PostgreSQL 只读查询
psycopg2-binary>=2.9.0
# 鲸语大脑免密加密快照
cryptography>=42.0

# ── 大型可选能力（不随核心自动安装，进程序后到「设置 → 可选能力」一键装）──
# 浏览器自动化：playwright + playwright install chromium（约 150MB）
# 本地语音转写：faster-whisper（模型约 300MB）
# Piper 本地离线语音：piper-tts[zh] g2pW sentence_stream unicode_rbnf（模型约 220MB）
# 二维码识别：pyzbar（另需系统 zbar 库）
# RAR 解压：rarfile（另需 unrar）
