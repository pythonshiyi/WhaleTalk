openai>=1.75.0
httpx>=0.27.0
# 可选（安装后可获得更精确的 token 估算；未安装时自动回退为字符数估算）
tiktoken>=0.7.0
# 可选（识别/托盘；未安装时菜单栏会提示安装）
pywin32>=306
# 可选：PDF 处理（pdf_extract / pdf_create，未安装时工具提示安装）
PyMuPDF>=1.24
reportlab>=4.0
# 可选：代码块语法高亮（未安装时代码块保持纯文本）
pygments>=2.15
# 可选：Office 文档读取（docx_read / pptx_read；python-docx 已用于 create_doc）
python-pptx>=0.6.23
# 可选：RSS 聚合（rss_fetch）
feedparser>=6.0
# 可选：二维码（qrcode 生成；识别 pyzbar 需系统 zbar，缺失时降级提示）
qrcode[pil]>=7.4
pyzbar>=0.1.9
# 可选：嵌入式 KV 存储（kv_store）
diskcache>=5.6
# 可选：音视频处理（media_ffmpeg，内置 ffmpeg 二进制）
imageio-ffmpeg>=0.4.9
# 可选：公众号写作工具 HTML 输出（wechat_writer/output.py）
markdown>=3.5
# 可选：系统托盘常驻（未安装时托盘功能自动禁用；需 Pillow 生成图标）
pystray>=0.19.5
# 可选：图片处理（image_process / image_understand / 屏幕截图）
Pillow>=10.0
# 可选：Office 文档（create_doc 的 .docx 支持 / docx_read）
python-docx>=1.1.0
# 可选：Excel 读写（read_excel / write_excel）
openpyxl>=3.1.0
# 可选：数据可视化图表（chart_data）
matplotlib>=3.8.0
# 可选：MySQL 只读查询（database_query_mysql）
PyMySQL>=1.1.0
# 可选：PostgreSQL 只读查询（database_query_postgres）
psycopg2-binary>=2.9.0
# 可选：本地语音转写（speech_to_text，faster-whisper）
faster-whisper>=1.0.0
# 可选：浏览器自动化（browser_navigate / web_screenshot / 有头预览；另需 playwright install chromium）
playwright>=1.40.0
# 可选：被墙站点抓取主路径（fetch_blocked，Chrome 指纹可过 Cloudflare；缺失时自动降级标准库 TLS-in-TLS）
curl_cffi>=0.7.0
# 可选：系统资源自检（system_status 的 CPU/内存详情；缺失时降级为磁盘+网络）
psutil>=5.9.0
